source 'https://rubygems.org'

gem 'thin'

gem 'rails', '~> 4.0.4'
gem 'rails_config'
gem 'jquery-rails'

gem 'sqlite3'
# gem 'bcrypt-ruby', '~> 3.0.0'

gem 'slim'
gem "batman-rails"

gem 'devise'

# Use SCSS for stylesheets
gem 'sass-rails', '~> 4.0.0'

gem 'coffee-rails'

# gem 'therubyracer', :platforms => :ruby

gem 'bootstrap-sass'

gem 'uglifier'

group :development do
  gem 'annotate' # Model annotations
  gem 'quiet_assets' # Disable asset requests in log
  # gem 'letter_opener', '~> 0.0.2' # Display emails in browser instead of sending it
  # gem 'proxylocal', '~> 0.3.1' # Make local application accessible from internet
  # gem 'guard', '~> 1.3.0' # file changes monitoring and tests execution
  # gem 'guard-spork', '~> 1.1.0'
  # gem 'guard-zeus'
  # gem 'guard-rspec', '~> 2.1.2'
  # gem 'zeus'
end

